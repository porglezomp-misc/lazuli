(struct cons
  car
  cdr)
(let! car cons/car)
(let! cdr cons/cdr)
(let! nil ())

(defn (print-elems ls)
  (if (null? ls)
    nil
    (begin
      (print (car ls))
      (print-elems (cdr ls)))))

(print-elems (quote (1 2 3 4 5 (6 7))))
